<% flash.each do |type, msg| %>
    <div class="ui <%= type %> mini message transition">
        <i class="close icon"></i>
        <div class="header">
            <%= msg %>
        </div>
    </div>
<% end %>